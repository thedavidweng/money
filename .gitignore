.DS_Store

# Local environment and secrets.
.env
.env.*
!.env.example

# Cursor MCP with local bearer tokens (do not commit).
.cursor/mcp.json

# Local runtime data and encrypted databases.
data/
tmp/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Build and test outputs.
bin/
dist/

# Frontend (Astro).
website/node_modules/
website/dist/
coverage.out
*.test


# Local-only donor repositories. Keep them out of git; this project is a clean rewrite.
donors/
