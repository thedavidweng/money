version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
  exclusions:
    presets:
      - std-error-handling
    paths:
      - third_party
      - vendor
      - testdata
      - website
